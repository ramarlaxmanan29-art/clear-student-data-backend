{
  "name": "clear-student-data-backend",
  "version": "1.0.0",
  "description": "Backend that deletes student Firebase Auth accounts + Firestore data when the admin panel presses Clear All Data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^14.3.0"
  }
}
